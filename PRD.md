# Happy Habit - PRD (Product Requirements Document)

> 작성일: 2026-08-12
> 버전: v0.1 (MVP)

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 프로젝트 이름 | Happy Habit (가제) |
| 한 줄 설명 | 습관 인증 서비스 |
| 유형 | 모바일 앱 (React Native + Expo) — 모바일 웹(반응형 웹앱)도 동일 코드베이스로 병행 지원 |
| 난이도 | 개인 토이 프로젝트 (혼자 쓰는 MVP), React Native 학습 목적 |

> 📌 참고: "Happy Habit"은 가제이며, 유사 서비스가 있을 수 있으니 최종 배포 전 상표/네이밍 확인 필요

---

## 2. 사용자 시나리오

| 항목 | 내용 |
|---|---|
| 누가 (Who) | 우선 본인 1인, 향후 불특정 다수 확장 고려 |
| 언제 (When) | 하루 마무리 시점 (자기 전, 저녁 등 — 실사용하며 검증 예정) |
| 왜 (Why) | 기존에 종이 다이어리/스티커 메모로 습관을 기록. 노션 등 도구는 복잡한 문법/기능으로 진입장벽이 있어, 더 단순하고 직관적인 기록 방식이 필요함 |

**핵심 차별점**: 복잡한 문법 없이, 종이에 스티커 붙이듯 단순하게 기록하는 경험

---

## 3. 핵심 기능 목록

### 필수 기능
- 습관 등록/관리
- 오늘 할 습관 체크
- 사진/텍스트 인증 기록 (강제 아님, 선택적으로 등록)
- 캘린더/히스토리로 지난 기록 보기
- 알림/리마인더
- 통계 (주간/월간/전체 달성률)

### 선택 기능 (MVP 이후 고려)
- 연속 달성일(스트릭) 표시 → 통계 화면에 한해 최소한으로 노출 고려
- 인증 기록 갤러리(모아보기) 화면
- 데이터 백업/복원 (수동 export)

---

## 4. 기술 스택

| 항목 | 내용 |
|---|---|
| 프레임워크 | React Native + Expo (managed workflow) |
| OTA 업데이트 | EAS Update (구 CodePush 개념의 현재 표준, 학습 목적 적용) |
| 로컬 저장소 | SQLite (expo-sqlite) |
| 알림 | 로컬 푸시 알림 (expo-notifications) |
| 플랫폼 | Android 주력, iOS 병행 고려, 모바일 웹(react-native-web) 동시 지원 |

> 📌 참고: CodePush는 2025년 Microsoft에 의해 deprecated 처리됨. 현재는 Expo의 EAS Update가 표준 대체재.
> 📌 참고: 웹은 별도 앱 설치 없이 브라우저로 바로 확인·공유하기 위한 용도. 스토어 배포가 없는 개인 프로젝트 특성상, 웹이 가장 접근성 높은 실사용/데모 경로가 될 수 있음.

---

## 5. 화면 구성

### 내비게이션
- 진입 화면: 홈 (오늘)
- 방식: 하단 탭바

### 구조
```
[탭바]
├── 🏠 홈 (오늘)
│     └── + 버튼 → 습관 등록/수정 (스택)
├── 📅 캘린더
│     └── 날짜 클릭 → 상세 기록 (스택)
├── 📊 통계
└── ⚙️ 설정
      └── 습관 관리 목록 → 습관 등록/수정 (스택)
```

| 탭 순서 | 화면 | 설명 |
|---|---|---|
| 1 | 홈 | 오늘 할 습관 체크, 진행률 표시 |
| 2 | 캘린더 | 캘린더/타임라인으로 지난 기록 조회 |
| 3 | 통계 | 주간/월간/전체 달성률 시각화 |
| 4 | 설정 | 알림 설정, 습관 관리 진입점, 앱 정보 |

---

## 6. 상세 기능 명세

### 6-1. 습관 등록/관리

**입력 항목**
| 항목 | 필수 여부 |
|---|---|
| 습관 이름 | 필수 |
| 아이콘 (사전 정의된 이모지 목록 중 선택) | 필수 |
| 카테고리 (건강/공부/기타 등) | 필수 |
| 메모/설명 | 선택 |

**반복 주기**: 매일 / 주 N회 (요일 무관) 중 선택

**인증 방식**: 전체 습관 공통 적용 — 기본 단순 체크, 선택적으로 사진/텍스트 기록 추가 가능

**습관 개수 제한**: 설정에서 단계별 선택 가능 (기본값: 1단계)

| 단계 | 이름(예시) | 최대 습관 개수 |
|---|---|---|
| 1단계 | 🌱 새싹 (기본값) | 3개 |
| 2단계 | 🌿 나무 | 5개 |
| 3단계 | 🌳 숲 | 7개 |
| 4단계 | ♾️ 자유 | 제한 없음 |

**삭제/보관**: 2단계 구조
- 그만두기(보관): 목록에서만 숨김, 지난 기록은 캘린더/통계에 유지 (기본 동작)
- 완전 삭제: 보관함에서 별도 실행, 되돌릴 수 없다는 경고 후 진행

---

### 6-2. 오늘 습관 체크

**체크 UI**
- 버튼 클릭 시 즉각적 시각 피드백 (체크 애니메이션 등)
- 화면 상단 오늘 진행률 프로그레스 바 (체크 시 실시간 반영)
- 선택적 팝업: "인증 기록을 남기시겠어요? [사진/메모 추가] [그냥 완료]"

**미완료 처리**
- 자정 기준 자동 마감 → 체크 안 한 습관은 "미체크"로 표시 (부정적 표현인 "실패" 대신 중립적 표현 사용)
- 캘린더/통계에 "미달성"으로 집계

**취소(되돌리기)**: 당일 항목은 토글 방식으로 체크 해제 가능

**홈 화면 표시 항목**
| 항목 | 포함 여부 |
|---|---|
| 오늘 진행률 % (프로그레스 바) | ✅ |
| 남은 습관 개수 | ✅ |
| 스트릭(연속 달성일) | ❌ (MVP 제외, 추후 통계 화면에 한해 고려) |

---

### 6-3. 캘린더/히스토리

| 항목 | 내용 |
|---|---|
| 캘린더 표시 | 날짜 칸에 그날 체크한 습관 아이콘을 작게 나열 |
| 날짜 클릭 시 | 그날 습관 목록 + 완료 여부 + 사진/텍스트 인증 기록 표시 |
| 지난 기록 수정 | 가능 (단, "수정됨" 표시 남김) |
| 조회 뷰 | 월별 캘린더 + 리스트(타임라인) 뷰 전환 가능 |
| 필터링 | 특정 습관만 선택해서 캘린더에 표시 가능 |

---

### 6-4. 알림/리마인더

| 항목 | 내용 |
|---|---|
| 알림 시점 | 습관별 개별 시간 지정 |
| 알림 내용 | 습관 이름 포함 (예: "🏃 운동할 시간이에요!") |
| 미완료 리마인더 | 하루 마무리 시점(예: 저녁 9시)에 "아직 체크 안 한 습관이 N개 있어요" 알림 |
| 알림 관리 | 습관별 개별 on/off + 전체 알림 일괄 on/off |

---

### 6-5. 통계

| 항목 | 내용 |
|---|---|
| 통계 지표 | 전체 평균 달성률, 습관별 개별 달성률, 요일별 패턴 |
| 시각화 | 도넛그래프(전체 평균) + 막대그래프(습관별/요일별) + 히트맵(누적 기록, 잔디밭 스타일) |
| 기간 선택 | 주간 / 월간 / 전체 기간(누적) |

---

### 6-6. 인증 기록 (선택 기능)

| 항목 | 내용 |
|---|---|
| 인증 방식 | 사진 + 텍스트 메모 둘 다 지원, 하나만 입력해도 무방 |
| 사진 소스 | 갤러리 선택 + 카메라 촬영 둘 다 지원 |
| 조회 방식 | 캘린더 상세 화면 + 타임라인(리스트) 뷰로 제공 (별도 갤러리 화면은 v2 이후 고려) |

---

## 7. 디자인 가이드

| 항목 | 내용 |
|---|---|
| 톤앤매너 | 귀엽고 캐주얼함 우선 + 따뜻한 손그림 느낌 |
| 색상 | 파스텔톤 (민트/라벤더/살구/크림 계열) |
| 폰트 | 배달의민족 한나체(제목) + 온글잎 박다현체(본문) + Pretendard(숫자/통계) |
| 배경/질감 | 도트·모눈종이 느낌의 은은한 배경 (스티커 다이어리 감성) |
| 귀여움 요소 | 아이콘/스티커 수준으로 한정 (캐릭터 시스템은 MVP 범위 밖) |
| 다크모드 | 지원 (저채도 별도 팔레트로 눈 피로도 최소화) |
| 기본 모드 | 라이트모드 |

---

## 8. 제약사항

| 항목 | 내용 |
|---|---|
| 개발 기간 | 명확한 마감 없음, 빠른 완성 지향 |
| 배포 계획 | 정식 스토어 출시 없음, EAS Update(OTA) 학습 목적으로 적용 |
| 예산 | 전부 무료 티어 사용 (Expo, EAS Update 무료 플랜) |
| 언어 | 한국어만 지원 (다국어 미지원) |
| 오프라인 지원 | 로컬 저장(SQLite) 기반이라 완전 오프라인 동작, 네트워크 연결 불필요. 웹 버전은 브라우저 저장소(WASM SQLite) 기반이라 최초 접속 시에는 페이지 로드에 네트워크가 필요하고, 저장 데이터는 기기/브라우저별로 분리됨 |
| 백업/복원 | MVP 범위 밖 (v2 이후 수동 export/import 기능 고려) |

---

## 향후 고려사항 (Out of Scope, v2 이후)

- 스트릭(연속 달성일) 표시
- 인증 기록 갤러리(모아보기) 화면
- 데이터 백업/복원 (수동 export/import)
- 색상 태그 등 습관별 추가 커스터마이징
- 서버 연동 및 다기기 동기화
- 다국어 지원
